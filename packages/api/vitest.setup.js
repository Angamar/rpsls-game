import { beforeAll, afterAll } from "vitest";
// Example: Mock global variables or setup database connections
beforeAll(() => {
  console.log("Setting up tests...");
});
afterAll(() => {
  console.log("Cleaning up after tests...");
});
